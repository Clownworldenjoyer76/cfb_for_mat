version: "1.0"

linter: qodana-python

profile:
  name: qodana.recommended

bootstrap: |
  python -m pip install --upgrade pip==26.2.1
  python -m pip install -r docs/win/football/cfb/requirements.txt

exclude:
  - name: All
    paths:
      - docs/win/football/cfb/05_backtest/output
      - docs/win/football/cfb/archive